import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    display: 'flex',
    width: '100%',
  },
  textInput: {
    borderWidth: 1,
    borderRadius: 5,
    padding: 10,
  },
});
